spring:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop

datasource:
  url: jdbc:h2:mem:nerdlab_test_db;DB_CLOSE_DELAY=-1
  username: sa
  password:
  driver-class-name: org.h2.Driver